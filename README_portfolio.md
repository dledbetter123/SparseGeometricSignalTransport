A geometric theory of the transformer: attention as parallel transport on a fiber bundle, not an O(T²) tax. Tokens become sparse Fourier constellations. It never beat GPT outright — but did surface a performant, drop-in curvature-based positional encoding that requires further study, though it's promising given the research direction.
